import styled from "styled-components";

export const Container = styled.div`
  margin-top: 3rem;
  padding: 0 4rem;

  img {
    width: 10rem;
    border-radius: 1rem;
  }

  .swiper-slide {
    width: auto;
  }

  .swiper-button-prev,
  .swiper-button-next {
    color: #fff;
    padding: 30px;
  }
`;

export const Title = styled.h2`
  margin-bottom: 1rem;
`;
